[Unit]
Description=Back up the Nutrilite Converter SQLite catalog
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker compose --env-file /opt/nutrilite-converter/.env -f /opt/nutrilite-converter/current/deploy/lightsail/compose.production.yaml --profile maintenance run --rm backup

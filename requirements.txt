# Nutrilite Converter intentionally uses only the Python standard library.
# This file is present so automated installers can use the conventional
# `python -m pip install -r requirements.txt` step without adding dependencies.
